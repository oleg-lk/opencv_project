      //1. убрать GetAsyncKeyState, реализовать через cv::waitKey(0).

      //cv::waitKey тоже возвращает нажатую кнопку. Кнопка "esc" равна 27

      //2. DataPhoto data перенести в начало функции main

      //У DataPhoto убрать наслденика View. Они разные сущности будут, не связанные никак между собой

      //3. Классу ViewSrc создать конструктор с параметров типа "имя окна".

      //В конструкторе проинициализировать cv::namedWindow с помощью этого параметра.

      //При этом cv::namedWindow в функции showFrame не нужна. cv::namedWindow отвечает за инициализацию окна

      //и это нужно сделать всего один раз, например в конструкторе.

      //4. В начале функции main создать два обьекта ViewSrc с разными названиями окон

      //Одно окно будет рисовать оригинальную картинку, другое измененное

      //5. Создать класс контроллер, в котором будут внутри храниться обьекты DataPhoto и два ViewSrc.

      //В начале функции main создать обьект класс контроллер и отправить туда

      //обьекты классов DataPhoto и два ViewSrc через set функции. В основном бесонечном цикле через функцию контроллера

      //будет происходить вся работа

      //

      //    while (true) {

      //    int key = cv::waitKey(0);

      //    ....

      // здесь проверка key

      //  ....

      //    controller.work();

      //}

      //

      //6. Функция work контроллера получает данные _data.getData(). Следом пусть она сожмет картинку

      //до какого-нибудь вменяемого размера,чтобы влезло в экран, использую cv::resize. Отрисовывает это в первое окно.

      //Далее изображение делает серым с помощью cv::cvtColor и выводит его во второе окно

